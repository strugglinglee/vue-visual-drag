export type ShapePonitType = "lt" | "rt" | "lb" | "rb" | "l" | "r" | "t" | "b";
// 每个点对应的初始角度
export enum InitialAngle {
  lt = 0,
  t = 45,
  rt = 90,
  r = 135,
  rb = 180,
  b = 225,
  lb = 27,
  l = 315,
}
